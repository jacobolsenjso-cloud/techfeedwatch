---
import MainLayout from '../layouts/MainLayout.astro';

const allVideos = Object.values(import.meta.glob('../content/videos/*.md', { eager: true }));

// Filtrerer kun Shorts og sorterer dem efter dato (nyeste først)
const shorts = allVideos
  .filter((v: any) => v.frontmatter.isShort === true)
  .sort((a: any, b: any) => new Date(b.frontmatter.date).valueOf() - new Date(a.frontmatter.date).valueOf());

const pageTitle = "Shorts";
---

<MainLayout title={pageTitle}>
  
  <style is:global>
    .shorts-wrapper { 
      padding: 30px 20px; 
      max-width: 1400px; 
      margin: 0 auto; 
      flex: 1; 
      width: 100%; 
      box-sizing: border-box; 
    }

    .header-title { 
      font-size: 2.2rem; 
      margin-top: 0;
      margin-bottom: 30px; 
      display: flex; 
      align-items: center; 
      gap: 12px; 
    }

    /* Shorts Grid i højkant */
    .shorts-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
    
    .short-card { text-decoration: none; color: inherit; display: block; transition: transform 0.3s ease; }
    .short-card:hover { transform: translateY(-4px); }
    
    .short-card-thumb { aspect-ratio: 9/16; background-color: #000; background-size: cover; background-position: center; border-radius: 12px; margin-bottom: 10px; position: relative; overflow: hidden; }
    
    /* Play knap svæve-effekt */
    .play-overlay { position: absolute; inset: 0; background: rgba(0,0,0,0.2); display: flex; align-items: center; justify-content: center; opacity: 0; transition: opacity 0.3s ease; }
    .play-icon { width: 48px; height: 48px; background: rgba(239, 68, 68, 0.9); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-size: 20px; padding-left: 4px; box-sizing: border-box; backdrop-filter: blur(4px); }
    .short-card:hover .play-overlay { opacity: 1; }

    .tma-grid-title { margin: 0 0 5px 0; font-size: 15px; line-height: 1.4; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; transition: color 0.2s; }
    .short-card:hover .tma-grid-title { color: #3b82f6; }
    
    .tma-grid-meta { font-size: 13px; color: #6b7280; margin: 0; }
    body.dark-mode .tma-grid-meta { color: #9ca3af; }

    .empty-msg { font-size: 1.1rem; color: #6b7280; margin-top: 20px; }
    body.dark-mode .empty-msg { color: #9ca3af; }

    @media (max-width: 768px) { 
      .shorts-wrapper { padding: 15px; }
      .header-title { font-size: 1.8rem; }
      .shorts-grid { grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 15px; }
    }
  </style>

  <div class="shorts-wrapper">
    <h1 class="header-title"><span style="color: #ef4444;">⚡</span> Alle Shorts</h1>
    
    <div class="shorts-grid">
      {shorts.map((short: any) => (
        <a href={`/video/${short.file.split('/').pop().replace('.md', '')}`} class="short-card">
          <div class="short-card-thumb" style={`background-image: url('https://img.youtube.com/vi/${short.frontmatter.youtubeId}/maxresdefault.jpg');`}>
            <div class="play-overlay"><div class="play-icon">▶</div></div>
          </div>
          <h3 class="tma-grid-title">{short.frontmatter.title}</h3>
          <p class="tma-grid-meta">Tech Feed Watch • {short.frontmatter.date}</p>
        </a>
      ))}
      
      {shorts.length === 0 && <p class="empty-msg">Ingen Shorts fundet endnu.</p>}
    </div>
  </div>
</MainLayout>